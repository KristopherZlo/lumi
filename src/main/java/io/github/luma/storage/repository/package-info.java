/**
 * Repository implementations for Luma persistence.
 *
 * <p>Repositories own on-disk formats, binary codecs, JSON manifests, atomic
 * writes, and file discovery. Domain services should treat this package as the
 * storage boundary instead of reimplementing file logic themselves.
 */
package io.github.luma.storage.repository;
